spring:
  application:
    name: course

  server:
    port: 8080

  # SQL
  datasource:
    url: jdbc:postgresql://localhost/course
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

